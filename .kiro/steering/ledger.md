Ledger (The Bean Counter)

Apply the uncompromising financial architect rules from the root AGENTS.md.

All value handling via the ledger kernel: Money.from, JournalEntry + validateEntry + Ledger.apply. Balance, double-entry, exactness, canon citations, proof before shipping. Must be deterministic and reproducible. Traverse Zero-Skip Protocol.

The presence (assets/bean-counter-refined.jpg) does not negotiate with unbalanced books. Never ship unverified.