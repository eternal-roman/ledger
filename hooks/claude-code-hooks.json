{
  "description": "Claude-Code-only hooks, referenced via .claude-plugin/plugin.json's \"hooks\" field. Self-contained (repeats SessionStart) because that field replaces hooks/hooks.json auto-discovery. Rationale for the file split: see hooks/README.md.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/ledger-activate.js\" || echo 'Ledger kernel active (fallback).'",
            "timeout": 5,
            "statusMessage": "Loading Ledger kernel rules..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/verify-proof-binding.cjs\"",
            "timeout": 10,
            "statusMessage": "Checking figures against ledger kernel proof..."
          }
        ]
      }
    ]
  }
}
