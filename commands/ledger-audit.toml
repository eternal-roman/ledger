description = "Adversarial whole-project financial invariants audit (kernel-powered, cross-language, transaction-trace enforced)"
prompt = "Execute the SUPER_LEDGER_AUDIT_PROTOCOL (docs/SUPER_LEDGER_AUDIT_PROTOCOL.md) verbatim. Bootstrap and prove the canonical first (Python reference-implementations/python/ledger/ or native ledger for TS). Build scanner, produce inventory of >=120 expressions. Rebuild every critical money lifecycle as exact JournalEntry sequences and REPLAY them step-by-step with Ledger.apply, capturing balances/equation/auditHash at each checkpoint. Generate side-by-side numeric counterexamples vs subject's native math. Run simulator, fuzzer, reconciliation. Self-attack. Deliver full LEDGER_SUPER_AUDIT_REPORT.md + all runnable artifacts + ENFORCER_VERIFICATION block. Quantitative bars are mandatory. Use only core primitives. No handwavy summaries. Double-Entry or Get Beta."