description = "Pre-ship review: kernel first, then host TDD/review/security"
prompt = "Run ledger-verify or ledger-audit. Then any host TDD/review/security skills. Pass/fail + ranked issues. If no host layers: 'Ledger layer only'. Do not ship if the ledger layer fails."
