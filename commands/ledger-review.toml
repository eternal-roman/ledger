description = "Full multi-layer ledger review: invariants + superpowers verification + pr-review-toolkit agents + security"
prompt = "Execute the complete ledger-review process: 1. Apply full ledger-verify or ledger-audit on the changes/diff/scope. Enforce all core primitives and the Canonical Financial Artifact. 2. Confirm superpowers TDD and verification steps were followed. 3. Invoke pr-review-toolkit agents (silent-failure-hunter, type-design-analyzer, pr-test-analyzer, code-reviewer). 4. Incorporate any security-guidance findings. 5. Produce a pass/fail summary with prioritized fixes. Only declare clean when every layer passes. Reference ledger skills and AGENTS.md throughout."
