description = "Seeded scenario with a kernel trace"
prompt = "Run this scenario on the kernel only. Seed any randomness. Log assumptions. Each step: validate, apply, prove equation. Output trace + auditHash."
