description = "IFRS/GAAP citation from the knowledge graph"
prompt = "Look up the concept in the ledger knowledge graph (cite_lookup / fetch). Return fact + source. Do not invent a paragraph. If values follow, they go through Money.from and a validated entry."
