description = "Verify a diff or file: Money.from, balance, citations, artifact"
prompt = "Verify this change against the Ledger kernel. Force Money.from for every amount. Build JournalEntry where value moves. Call validateEntry / Ledger.apply. Report location + issue + fix. Require a real auditHash on any artifact. Do not pass unbalanced or float math."
