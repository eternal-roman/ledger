# Host plugin

```bash
grok plugin install /path/to/ledger --trust
```

Or copy `AGENTS.md` / `skills/ledger/SKILL.md` and use `.claude-plugin/` if the host reads that manifest.

`npx -y @eternal-roman/ledger-mcp` is the host-agnostic path. See the root README.
